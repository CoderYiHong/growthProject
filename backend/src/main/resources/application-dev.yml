# ============================================================
# 本地开发配置 — 仅在 spring.profiles.active=dev 时加载
# 启动方式：mvn spring-boot:run -Dspring-boot.run.profiles=dev
# ============================================================
# 安全：此文件只允许本地开发使用，禁止部署到生产环境
# 生产环境通过环境变量 ADMIN_PASSWORD_HASH / ADMIN_TOKEN_SECRET 配置
# ============================================================

admin:
  password-hash: ${ADMIN_PASSWORD_HASH:$2a$10$Ahig3ku2jZl8kfxtJ/v/Neq5OdPJtVs22h3ZnO9OJqvbAJvGcZ3QG}
  token-secret: ${ADMIN_TOKEN_SECRET:local-dev-token-secret-not-for-production-use-42chars}

cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

spring:
  sql:
    init:
      mode: always
