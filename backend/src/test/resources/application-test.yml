# 测试 profile — 使用 H2 内存数据库，不依赖 MySQL
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

admin:
  username: admin
  password-hash: $2a$10$Ahig3ku2jZl8kfxtJ/v/Neq5OdPJtVs22h3ZnO9OJqvbAJvGcZ3QG
  token-secret: test-secret-key-for-automated-tests-only-42chars
  token-expire-hours: 1

cors:
  allowed-origins: http://localhost:5173

contact:
  receive-email: test@localhost
